# ~/.bash_functions
# Use `_` in front of function name
# Generate alias to normalize

# ── System Update ─────────────────────────────────────────────────────────────────

_update() {
	set -e
	sudo apt update
	sudo apt full-upgrade -y
	sudo apt autoremove -y
	sudo apt autoclean -y
	echo 'System update complete.'
	set +e
}
